{
    "version": 2,
    "build": {
        "src": "./src/server.ts",
        "use": "@now/node"
    },
    "routes": [
        {
            "src": "(.*)",
            "dest": "/src/server.ts"
        }
    ]
}
